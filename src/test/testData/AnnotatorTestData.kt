/**
 * ```kotlin doctest
 * foo()
 * ```
 *
 * ```kotlin doctest<warning>:</warning>
 * foo()
 * ```
 *
 * ```kotlin doctest:run
 * foo()
 * ```
 *
 * ```kotlin doctest:<warning>unknown</warning>
 * foo()
 * ```
 *
 * ```kotlin doctest<warning>?</warning>
 * foo()
 * ```
 */
fun foo() = "foo"
